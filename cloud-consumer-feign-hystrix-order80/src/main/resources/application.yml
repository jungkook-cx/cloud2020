server:
  port: 80

eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/

#设置feign客户端超时时间
ribbon:
  #建立连接之后读取的时间
  ReadTimeout: 5000
  #指的是建立连接所用的时间
  ConnectTimeout: 5000

feign:
  hystrix:
    enabled: true